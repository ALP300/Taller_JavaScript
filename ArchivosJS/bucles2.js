//Solicitar al usuario que ingrese una palabra y luego 
//imprimir cada letra en una línea diferente utilizando un bucle for.
let palabra= prompt("Ingrese una palabra: "); //HOLA COMO ESTÁN 
for(let letra of palabra){
    console.log(letra);
}
